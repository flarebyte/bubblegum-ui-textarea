#!/bin/sh
# Generated with make generate. See template script/template/big-test.hbs

echo "Run the browser tests ..."

echo "You need to start the server first: cd docs;http-server -p 7000 -c10 -o"
lsof -nP -iTCP -sTCP:LISTEN | grep -q 7000

npx playwright test